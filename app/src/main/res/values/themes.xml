<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base App Theme (Material3 for other activities) -->
    <style name="Base.Theme.BazmERaah" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/teal_700</item>
        <item name="colorPrimaryVariant">@color/teal_700</item>
        <item name="colorSecondary">@color/teal_700</item>
        <item name="android:windowBackground">@color/lighb</item>
    </style>

    <!-- SplashActivity Theme (AppCompat to avoid system splash) -->
    <style name="Theme.BazmERaah.Transparent" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>


</resources>