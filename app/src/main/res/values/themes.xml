<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base Theme (Light) -->
    <style name="Base.Theme.BazmERaah" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- App ki global customization -->
        <item name="colorPrimary">@color/teal_700</item>
        <item name="colorPrimaryVariant">@color/teal_700</item>
        <item name="colorSecondary">@color/teal_700</item>
    </style>

    <!-- Normal Theme -->
    <style name="Theme.BazmERaah" parent="Base.Theme.BazmERaah" />

    <!-- Splash Theme -->
    <style name="Theme.BazmERaah.Splash" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
    </style>

</resources>
