<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Base.Theme.BazmERaah" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Dark mode colors -->
        <item name="colorPrimary">@color/teal_700</item>
        <item name="colorSecondary">@color/teal_700</item>
        <item name="android:windowBackground">@android:color/black</item>
        - یہاں change کرو -->
        <item name="colorOnPrimary">@color/light_gray</item>
        <item name="android:textColor">@color/teal_200</item>
        <item name="cardBackgroundColor">@color/gr</item>
        <item name="hintTextColor">@color/light_gray</item>
    </style>

    <style name="Theme.BazmERaah" parent="Base.Theme.BazmERaah" />

    <style name="Theme.BazmERaah.Transparent" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        !-- یہاں بھی black -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

</resources>
